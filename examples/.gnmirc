metadata:
    username = "admin"
    password = ""

get:
    options:
        encoding: "json"

subscribe:
    options:
        aggregate: false
        encoding: "json"
        mode: "stream"
        submode: "on-change"
        suppress: true
        # qos: 0 
        # interval:
        # heartbeat:
        # timeout:
